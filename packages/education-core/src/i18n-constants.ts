/**
 * Platform locale constants, inlined here so education-core does not depend on
 * any single app's `@/i18n/routing`. These mirror the platform-wide values.
 */
export const defaultLocale = 'en' as const;
export const supportedLocales = ['en', 'vi'] as const;
