# TanStack/Rust Cutover Runbook

The full dual-stack cutover runbook now lives in the discoverable docs app:

- `apps/docs/build/devops/tanstack-rust-cutover-runbook.mdx`

Use that page for local and CI dual-stack bring-up, Docker health semantics,
E2E execution, cutover gates, benchmark evidence, and rollback steps. This root
file is kept only as a compatibility pointer for older links and must not
contain copied manifest counts or operational snapshots.

Related canonical docs:

- Migration plan:
  `apps/docs/platform/architecture/tanstack-rust-migration-plan.mdx`
- Architecture and route ownership contract:
  `apps/docs/platform/architecture/tanstack-rust-migration.mdx`
