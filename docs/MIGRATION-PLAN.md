# TanStack/Rust Migration Plan

The full migration plan now lives in the discoverable docs app:

- `apps/docs/platform/architecture/tanstack-rust-migration-plan.mdx`

Use that page for the current route-manifest snapshot, sequencing, verification
gates, and agent-lane planning. This root file is kept only as a compatibility
pointer for older links and must not contain copied manifest counts.

Related canonical docs:

- Architecture and route ownership contract:
  `apps/docs/platform/architecture/tanstack-rust-migration.mdx`
- Cutover runbook:
  `apps/docs/build/devops/tanstack-rust-cutover-runbook.mdx`
