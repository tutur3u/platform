# Skill Reference Scope

This directory contains a reusable rule catalog. Apply only rules relevant to the
requested change and the installed framework version; examples are not requirements
to refactor unrelated code or replace established project conventions.

Use `SKILL.md` to select individual `rules/` references. The complete catalog is in
[references/compiled-rules.md](references/compiled-rules.md) when a broad review needs it.
