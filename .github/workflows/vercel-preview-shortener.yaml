name: Vercel Shortener Preview Deployment
on:
  workflow_run:
    workflows: [ "Detect Changes for Apps" ]
    types: [ completed ]
    branches-ignore: [ production ]

jobs:
  check-changes:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    outputs:
      shortener_detected: ${{ steps.check-shortener.outputs.shortener_detected }}
      project_id: ${{ steps.extract-project-id.outputs.project_id }}
    steps:
    - name: Download apps-to-build artifact
      uses: actions/download-artifact@v4
      with:
        name: apps-to-build
        path: ./
        github-token: ${{ secrets.GITHUB_TOKEN }}
        run-id: ${{ github.event.workflow_run.id }}

    - name: Check if shortener should be built
      id: check-shortener
      run: |
        if [ -f "apps-to-build.txt" ]; then
          if grep -q "shortener" "apps-to-build.txt"; then
            echo "shortener_detected=true" >> $GITHUB_OUTPUT
            echo "✅ Shortener changes detected, proceeding with build"
          else
            echo "shortener_detected=false" >> $GITHUB_OUTPUT
            echo "❌ No Shortener changes detected, skipping build"
          fi
        else
          echo "shortener_detected=false" >> $GITHUB_OUTPUT
          echo "❌ apps-to-build.txt not found, skipping build"
        fi

    - name: Extract project ID
      id: extract-project-id
      env:
        PROJECT_ID: ${{ secrets.VERCEL_SHORTENER_PROJECT_ID }}
      run: |
        echo "project_id=$PROJECT_ID" >> $GITHUB_OUTPUT

  deploy-shortener:
    needs: check-changes
    if: needs.check-changes.outputs.shortener_detected == 'true'
    uses: ./.github/workflows/build-app.yml
    with:
      vercel-project-id: ${{ needs.check-changes.outputs.project_id }}
    secrets: inherit
