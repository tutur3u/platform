name: Check and Bump Package Versions

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Explain retired workflow
        run: |
          echo "::notice::Package version bumps are now generated by release-please."
          echo "::notice::Use .github/workflows/release-please.yaml on production instead."
