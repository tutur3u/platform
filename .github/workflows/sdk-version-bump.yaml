name: SDK Version Bump

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Explain retired workflow
        run: |
          echo "::notice::SDK version bumps are now generated by release-please."
          echo "::notice::Use .github/workflows/release-please.yaml on production instead."
