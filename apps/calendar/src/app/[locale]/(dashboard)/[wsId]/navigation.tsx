import type { NavLink } from '@tuturuuu/ui/custom/navigation';

export type { NavLink } from '@tuturuuu/ui/custom/navigation';

export async function getNavigationLinks(): Promise<(NavLink | null)[]> {
  return [];
}
