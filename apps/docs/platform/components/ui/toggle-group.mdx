---
title: "Toggle Group"
description: "Preview Toggle Group, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/toggle-group."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/toggle-group](https://tuturuuu.com/ui/components/toggle-group).
</Info>

**Category:** Actions · **Import:** `@tuturuuu/ui/toggle-group` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { ToggleGroup, ToggleGroupItem } from '@tuturuuu/ui/toggle-group';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { ToggleGroup, ToggleGroupItem } from '@tuturuuu/ui/toggle-group';

<ToggleGroup type="single" defaultValue="list"><ToggleGroupItem value="list">List</ToggleGroupItem></ToggleGroup>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { ToggleGroup, ToggleGroupItem } from '@tuturuuu/ui/toggle-group';

<ToggleGroup type="single" defaultValue="list"><ToggleGroupItem value="list">List</ToggleGroupItem></ToggleGroup>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  ToggleGroup,
  ToggleGroupItem,
} from '@tuturuuu/ui/toggle-group';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { ToggleGroup } from '@tuturuuu/ui/toggle-group';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `ToggleGroup` | component |
| `ToggleGroupItem` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Type
- Selection
- Size

## Related components

- [Button](./button)
- [Toggle](./toggle)
- [Accordion](./accordion)
