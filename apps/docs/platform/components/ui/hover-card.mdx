---
title: "Hover Card"
description: "Preview Hover Card, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/hover-card."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/hover-card](https://tuturuuu.com/ui/components/hover-card).
</Info>

**Category:** Overlays · **Import:** `@tuturuuu/ui/hover-card` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { HoverCard, HoverCardTrigger, HoverCardContent } from '@tuturuuu/ui/hover-card';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { HoverCard, HoverCardContent, HoverCardTrigger } from '@tuturuuu/ui/hover-card';

<HoverCard>
  <HoverCardTrigger>Hover profile</HoverCardTrigger>
  <HoverCardContent>Profile summary</HoverCardContent>
</HoverCard>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { HoverCard, HoverCardContent, HoverCardTrigger } from '@tuturuuu/ui/hover-card';

<HoverCard>
  <HoverCardTrigger>Hover profile</HoverCardTrigger>
  <HoverCardContent>Profile summary</HoverCardContent>
</HoverCard>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  HoverCard,
  HoverCardTrigger,
  HoverCardContent,
} from '@tuturuuu/ui/hover-card';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { HoverCard } from '@tuturuuu/ui/hover-card';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `HoverCard` | component |
| `HoverCardTrigger` | component |
| `HoverCardContent` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Delay
- Side
- Content

## Related components

- [Alert Dialog](./alert-dialog)
- [Context Menu](./context-menu)
- [Dialog](./dialog)
