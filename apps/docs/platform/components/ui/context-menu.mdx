---
title: "Context Menu"
description: "Preview Context Menu, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/context-menu."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/context-menu](https://tuturuuu.com/ui/components/context-menu).
</Info>

**Category:** Overlays · **Import:** `@tuturuuu/ui/context-menu` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { ContextMenu, ContextMenuTrigger, ContextMenuContent } from '@tuturuuu/ui/context-menu';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { ContextMenu, ContextMenuContent, ContextMenuItem, ContextMenuTrigger } from '@tuturuuu/ui/context-menu';

<ContextMenu>
  <ContextMenuTrigger>Right click area</ContextMenuTrigger>
  <ContextMenuContent><ContextMenuItem>Duplicate</ContextMenuItem></ContextMenuContent>
</ContextMenu>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { ContextMenu, ContextMenuContent, ContextMenuItem, ContextMenuTrigger } from '@tuturuuu/ui/context-menu';

<ContextMenu>
  <ContextMenuTrigger>Right click area</ContextMenuTrigger>
  <ContextMenuContent><ContextMenuItem>Duplicate</ContextMenuItem></ContextMenuContent>
</ContextMenu>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  ContextMenu,
  ContextMenuTrigger,
  ContextMenuContent,
} from '@tuturuuu/ui/context-menu';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { ContextMenu } from '@tuturuuu/ui/context-menu';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `ContextMenu` | component |
| `ContextMenuTrigger` | component |
| `ContextMenuContent` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Items
- Shortcuts
- Danger zone

## Related components

- [Alert Dialog](./alert-dialog)
- [Dialog](./dialog)
- [Drawer](./drawer)
