---
title: "Alert Dialog"
description: "Preview Alert Dialog, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/alert-dialog."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/alert-dialog](https://tuturuuu.com/ui/components/alert-dialog).
</Info>

**Category:** Overlays · **Import:** `@tuturuuu/ui/alert-dialog` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { AlertDialog, AlertDialogTrigger, AlertDialogContent } from '@tuturuuu/ui/alert-dialog';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { AlertDialog, AlertDialogContent, AlertDialogTrigger } from '@tuturuuu/ui/alert-dialog';

<AlertDialog>
  <AlertDialogTrigger>Open confirmation</AlertDialogTrigger>
  <AlertDialogContent>Confirm the irreversible action.</AlertDialogContent>
</AlertDialog>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { AlertDialog, AlertDialogContent, AlertDialogTrigger } from '@tuturuuu/ui/alert-dialog';

<AlertDialog>
  <AlertDialogTrigger>Open confirmation</AlertDialogTrigger>
  <AlertDialogContent>Confirm the irreversible action.</AlertDialogContent>
</AlertDialog>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  AlertDialog,
  AlertDialogTrigger,
  AlertDialogContent,
} from '@tuturuuu/ui/alert-dialog';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { AlertDialog } from '@tuturuuu/ui/alert-dialog';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `AlertDialog` | component |
| `AlertDialogTrigger` | component |
| `AlertDialogContent` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Trigger
- Destructive action
- Copy

## Related components

- [Context Menu](./context-menu)
- [Dialog](./dialog)
- [Drawer](./drawer)
