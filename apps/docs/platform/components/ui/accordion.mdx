---
title: "Accordion"
description: "Preview Accordion, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/accordion."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/accordion](https://tuturuuu.com/ui/components/accordion).
</Info>

**Category:** Layout · **Import:** `@tuturuuu/ui/accordion` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@tuturuuu/ui/accordion';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from '@tuturuuu/ui/accordion';

<Accordion type="single" collapsible>
  <AccordionItem value="overview">
    <AccordionTrigger>Overview</AccordionTrigger>
    <AccordionContent>Composable disclosure content.</AccordionContent>
  </AccordionItem>
</Accordion>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from '@tuturuuu/ui/accordion';

<Accordion type="single" collapsible>
  <AccordionItem value="overview">
    <AccordionTrigger>Overview</AccordionTrigger>
    <AccordionContent>Composable disclosure content.</AccordionContent>
  </AccordionItem>
</Accordion>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from '@tuturuuu/ui/accordion';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { Accordion } from '@tuturuuu/ui/accordion';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `Accordion` | component |
| `AccordionItem` | component |
| `AccordionTrigger` | component |
| `AccordionContent` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Type
- Collapsible
- Content density

## Related components

- [Aspect Ratio](./aspect-ratio)
- [Card](./card)
- [Carousel](./carousel)
