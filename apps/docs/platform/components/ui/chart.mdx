---
title: "Chart"
description: "Preview Chart, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/chart."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/chart](https://tuturuuu.com/ui/components/chart).
</Info>

**Category:** Data · **Import:** `@tuturuuu/ui/chart` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { ChartContainer, ChartTooltip, ChartTooltipContent } from '@tuturuuu/ui/chart';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { ChartContainer, ChartTooltip, ChartTooltipContent } from '@tuturuuu/ui/chart';

<ChartContainer config={{ work: { label: 'Work', color: 'hsl(var(--primary))' } }}>
  <BarChart data={data}>
    <ChartTooltip content={<ChartTooltipContent />} />
  </BarChart>
</ChartContainer>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { ChartContainer, ChartTooltip, ChartTooltipContent } from '@tuturuuu/ui/chart';

<ChartContainer config={{ work: { label: 'Work', color: 'hsl(var(--primary))' } }}>
  <BarChart data={data}>
    <ChartTooltip content={<ChartTooltipContent />} />
  </BarChart>
</ChartContainer>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
} from '@tuturuuu/ui/chart';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { ChartContainer } from '@tuturuuu/ui/chart';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `ChartContainer` | component |
| `ChartTooltip` | component |
| `ChartTooltipContent` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Series
- Theme
- Tooltip

## Related components

- [Diff Viewer](./diff-viewer)
- [Table](./table)
- [Accordion](./accordion)
