---
title: "Carousel"
description: "Preview Carousel, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/carousel."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/carousel](https://tuturuuu.com/ui/components/carousel).
</Info>

**Category:** Layout · **Import:** `@tuturuuu/ui/carousel` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { Carousel, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious } from '@tuturuuu/ui/carousel';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { Carousel, CarouselContent, CarouselItem } from '@tuturuuu/ui/carousel';

<Carousel opts={{ align: 'start' }}>
  <CarouselContent>
    <CarouselItem>Slide content</CarouselItem>
  </CarouselContent>
</Carousel>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { Carousel, CarouselContent, CarouselItem } from '@tuturuuu/ui/carousel';

<Carousel opts={{ align: 'start' }}>
  <CarouselContent>
    <CarouselItem>Slide content</CarouselItem>
  </CarouselContent>
</Carousel>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  Carousel,
  CarouselContent,
  CarouselItem,
  CarouselNext,
  CarouselPrevious,
} from '@tuturuuu/ui/carousel';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { Carousel } from '@tuturuuu/ui/carousel';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `Carousel` | component |
| `CarouselContent` | component |
| `CarouselItem` | component |
| `CarouselNext` | component |
| `CarouselPrevious` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Orientation
- Slides
- Controls

## Related components

- [Accordion](./accordion)
- [Aspect Ratio](./aspect-ratio)
- [Card](./card)
