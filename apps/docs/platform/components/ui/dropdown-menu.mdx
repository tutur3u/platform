---
title: "Dropdown Menu"
description: "Preview Dropdown Menu, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/dropdown-menu."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/dropdown-menu](https://tuturuuu.com/ui/components/dropdown-menu).
</Info>

**Category:** Overlays · **Import:** `@tuturuuu/ui/dropdown-menu` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent } from '@tuturuuu/ui/dropdown-menu';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from '@tuturuuu/ui/dropdown-menu';

<DropdownMenu>
  <DropdownMenuTrigger>Open menu</DropdownMenuTrigger>
  <DropdownMenuContent><DropdownMenuItem>Settings</DropdownMenuItem></DropdownMenuContent>
</DropdownMenu>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from '@tuturuuu/ui/dropdown-menu';

<DropdownMenu>
  <DropdownMenuTrigger>Open menu</DropdownMenuTrigger>
  <DropdownMenuContent><DropdownMenuItem>Settings</DropdownMenuItem></DropdownMenuContent>
</DropdownMenu>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
} from '@tuturuuu/ui/dropdown-menu';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { DropdownMenu } from '@tuturuuu/ui/dropdown-menu';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `DropdownMenu` | component |
| `DropdownMenuTrigger` | component |
| `DropdownMenuContent` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Items
- Checkbox items
- Shortcuts

## Related components

- [Alert Dialog](./alert-dialog)
- [Context Menu](./context-menu)
- [Dialog](./dialog)
