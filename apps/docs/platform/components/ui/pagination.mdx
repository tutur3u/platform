---
title: "Pagination"
description: "Preview Pagination, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/pagination."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/pagination](https://tuturuuu.com/ui/components/pagination).
</Info>

**Category:** Navigation · **Import:** `@tuturuuu/ui/pagination` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { Pagination, PaginationContent, PaginationItem, PaginationLink } from '@tuturuuu/ui/pagination';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { Pagination, PaginationContent, PaginationItem, PaginationLink } from '@tuturuuu/ui/pagination';

<Pagination><PaginationContent><PaginationItem><PaginationLink href="#">1</PaginationLink></PaginationItem></PaginationContent></Pagination>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { Pagination, PaginationContent, PaginationItem, PaginationLink } from '@tuturuuu/ui/pagination';

<Pagination><PaginationContent><PaginationItem><PaginationLink href="#">1</PaginationLink></PaginationItem></PaginationContent></Pagination>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  Pagination,
  PaginationContent,
  PaginationItem,
  PaginationLink,
} from '@tuturuuu/ui/pagination';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { Pagination } from '@tuturuuu/ui/pagination';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `Pagination` | component |
| `PaginationContent` | component |
| `PaginationItem` | component |
| `PaginationLink` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Page
- Siblings
- Disabled state

## Related components

- [Breadcrumb](./breadcrumb)
- [Command](./command)
- [Quick Command Center](./quick-command-center)
