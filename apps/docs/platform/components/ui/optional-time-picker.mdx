---
title: "Optional Time Picker"
description: "Preview Optional Time Picker, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/optional-time-picker."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/optional-time-picker](https://tuturuuu.com/ui/components/optional-time-picker).
</Info>

**Category:** Inputs · **Import:** `@tuturuuu/ui/optional-time-picker` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { OptionalTimePicker } from '@tuturuuu/ui/optional-time-picker';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { OptionalTimePicker } from '@tuturuuu/ui/optional-time-picker';

const [includeTime, setIncludeTime] = useState(true);

<OptionalTimePicker
  date={date}
  setDate={setDate}
  includeTime={includeTime}
  setIncludeTime={setIncludeTime}
  includeTimeLabel="Include time"
/>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { OptionalTimePicker } from '@tuturuuu/ui/optional-time-picker';

const [includeTime, setIncludeTime] = useState(true);

<OptionalTimePicker
  date={date}
  setDate={setDate}
  includeTime={includeTime}
  setIncludeTime={setIncludeTime}
  includeTimeLabel="Include time"
/>
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { OptionalTimePicker } from '@tuturuuu/ui/optional-time-picker';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `OptionalTimePicker` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Include time
- Timezone
- Time format
- Disabled state

## Related components

- [Calendar](./calendar)
- [Checkbox](./checkbox)
- [Color Picker](./color-picker)
