---
title: "Collapsible"
description: "Preview Collapsible, copy setup snippets, review examples, and inspect API exports for @tuturuuu/ui/collapsible."
---

{/* AUTO-GENERATED by scripts/generate-ui-component-docs.ts — do not edit by hand. */}

<Info>
  Explore the live, interactive preview at [tuturuuu.com/ui/components/collapsible](https://tuturuuu.com/ui/components/collapsible).
</Info>

**Category:** Layout · **Import:** `@tuturuuu/ui/collapsible` · **Status:** Live preview

## Installation

Install the shared UI package, import global styles once, then import the component from its public subpath.

```bash
bun add @tuturuuu/ui
```

```tsx
import '@tuturuuu/ui/globals.css';
import { Collapsible, CollapsibleTrigger, CollapsibleContent } from '@tuturuuu/ui/collapsible';
```

## Usage

Copy the default usage snippet and adapt labels, state, and event handlers to the product surface.

```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from '@tuturuuu/ui/collapsible';

<Collapsible>
  <CollapsibleTrigger>Toggle details</CollapsibleTrigger>
  <CollapsibleContent>Hidden content</CollapsibleContent>
</Collapsible>
```

## Examples

Examples reuse the registry snippets so the docs stay aligned with the component catalog.

### Default

A minimal implementation using the current registry snippet.

```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from '@tuturuuu/ui/collapsible';

<Collapsible>
  <CollapsibleTrigger>Toggle details</CollapsibleTrigger>
  <CollapsibleContent>Hidden content</CollapsibleContent>
</Collapsible>
```

### Composition

Use the grouped exports when the primitive has slots, triggers, content regions, or providers.

```tsx
import {
  Collapsible,
  CollapsibleTrigger,
  CollapsibleContent,
} from '@tuturuuu/ui/collapsible';
```

### Package import

Import through the public package path so workspace and external apps share the same contract.

```tsx
// Public package import
import { Collapsible } from '@tuturuuu/ui/collapsible';

// Workspace apps should keep importing through the package boundary.
```

## API reference

The public exports currently available from this component entry.

| Export | Kind |
| ------ | ---- |
| `Collapsible` | component |
| `CollapsibleTrigger` | component |
| `CollapsibleContent` | component |

## Customization

Common props, states, and composition points to review before using this component.

- Open state
- Trigger
- Content density

## Related components

- [Accordion](./accordion)
- [Aspect Ratio](./aspect-ratio)
- [Card](./card)
