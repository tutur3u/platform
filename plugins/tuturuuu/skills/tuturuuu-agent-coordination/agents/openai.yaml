interface:
  display_name: "Tuturuuu Agent Coordination"
  short_description: "Coordinate shared checkouts, worktrees, and branches across harnesses"
  default_prompt: "Coordinate this shared-checkout task across concurrent agents and worktrees, then commit safely and archive the note."
